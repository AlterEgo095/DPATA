# PlagiatIA - Configuration Nginx Production
# Université de Kinshasa (UNIKIN)
#
# Cette configuration est optimisée pour:
# - Next.js 16 avec Turbopack
# - SSL/TLS avec HTTP/2
# - Rate limiting avancé
# - Cache agressif des assets statiques
# - Sécurité renforcée (headers, CORS)
#
# Installation:
#   sudo cp plagiatia.conf /etc/nginx/sites-available/
#   sudo ln -s /etc/nginx/sites-available/plagiatia.conf /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx

#===============================================================================
# ZONES DE RATE LIMITING
#===============================================================================

# API générale: 100 requêtes/seconde par IP
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=100r/s;

# Login: 5 requêtes/minute (protection brute-force)
limit_req_zone $binary_remote_addr zone=login_limit:10m rate=5r/m;

# Upload: 10 requêtes/5 minutes
limit_req_zone $binary_remote_addr zone=upload_limit:10m rate=10r/5m;

# Connexions simultanées: max 20 par IP
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

#===============================================================================
# UPSTREAM - Backend Next.js
#===============================================================================

upstream plagiatia_backend {
    server 127.0.0.1:3000;
    
    # Keep-alive pour améliorer les performances
    keepalive 64;
    
    # Timeout de connexion au backend
    keepalive_timeout 60s;
    
    # Tentatives en cas d'échec
    max_fails=3;
    fail_timeout=30s;
}

#===============================================================================
# SERVER BLOCK - HTTP (Redirect vers HTTPS après SSL)
#===============================================================================

server {
    listen 80;
    listen [::]:80;
    server_name _;  # Remplacer par votre domaine
    
    # Logs
    access_log /var/log/nginx/plagiatia_access.log combined buffer=512k flush=1m;
    error_log /var/log/nginx/plagiatia_error.log warn;
    
    # ACME Challenge pour Certbot (avant SSL)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        allow all;
        default_type "application/octet-stream";
    }
    
    # Redirection vers HTTPS (décommenter après configuration SSL)
    # return 301 https://$host$request_uri;
    
    # Configuration proxy temporaire (avant SSL)
    include /etc/nginx/snippets/plagiatia-proxy.conf;
}

#===============================================================================
# SERVER BLOCK - HTTPS (après Certbot)
#===============================================================================

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name _;  # Remplacer par votre domaine
    
    #===========================================================================
    # CERTIFICATS SSL (générés par Certbot)
    #===========================================================================
    
    ssl_certificate /etc/letsencrypt/live/VOTRE_DOMAINE/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/VOTRE_DOMAINE/privkey.pem;
    
    #===========================================================================
    # CONFIGURATION SSL OPTIMISÉE
    #===========================================================================
    
    # Session SSL
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_session_tickets off;
    
    # Protocoles modernes uniquement
    ssl_protocols TLSv1.2 TLSv1.3;
    
    # Cipher suites sécurisées
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    
    # OCSP Stapling
    ssl_stapling on;
    ssl_stapling_verify on;
    resolver 8.8.8.8 8.8.4.4 valid=300s;
    resolver_timeout 5s;
    
    #===========================================================================
    # HEADERS DE SÉCURITÉ
    #===========================================================================
    
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' fonts.googleapis.com; font-src 'self' fonts.gstatic.com data:; img-src 'self' data: blob:; connect-src 'self' wss:;" always;
    
    # Cache-Control pour les réponses sans header explicite
    add_header Cache-Control "no-store, must-revalidate";
    
    # Logs
    access_log /var/log/nginx/plagiatia_ssl_access.log combined buffer=512k flush=1m;
    error_log /var/log/nginx/plagiatia_ssl_error.log warn;
    
    #===========================================================================
    # LOCATIONS
    #===========================================================================
    
    # Page racine et routes Next.js
    location / {
        limit_conn conn_limit;
        
        proxy_pass http://plagiatia_backend;
        include /etc/nginx/snippets/plagiatia-proxy.conf;
        
        # Ne pas cacher les pages dynamiques
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }
    
    # API endpoints - Rate limiting strict
    location /api/ {
        limit_req zone=api_limit burst=50 nodelay;
        limit_req_status 429;
        limit_conn conn_limit;
        
        proxy_pass http://plagiatia_backend;
        include /etc/nginx/snippets/plagiatia-proxy.conf;
        
        # Pas de cache pour l'API
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }
    
    # Login endpoint - Protection anti brute-force
    location /api/auth/login {
        limit_req zone=login_limit burst=3 nodelay;
        limit_req_status 429;
        
        proxy_pass http://plagiatia_backend;
        include /etc/nginx/snippets/plagiatia-proxy.conf;
        
        # Délai artificiel en cas de dépassement (anti-timing attacks)
        limit_req_status 429;
        error_page 429 = @login_rate_limited;
    }
    
    # Upload endpoint
    location /api/documents {
        limit_req zone=upload_limit burst=3 nodelay;
        limit_req_status 429;
        
        # Augmenter la taille max du body pour les uploads
        client_max_body_size 100M;
        
        proxy_pass http://plagiatia_backend;
        include /etc/nginx/snippets/plagiatia-proxy.conf;
        
        # Timeout augmentés pour les gros fichiers
        proxy_connect_timeout 120s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }
    
    # Assets statiques Next.js - Cache agressif
    location /_next/static/ {
        proxy_pass http://plagiatia_backend;
        
        expires 365d;
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options nosniff;
        access_log off;
    }
    
    # Images
    location ~* \.(ico|png|jpg|jpeg|gif|webp|avif|svg)$ {
        proxy_pass http://plagiatia_backend;
        
        expires 30d;
        add_header Cache-Control "public, no-transform";
        add_header X-Content-Type-Options nosniff;
        access_log off;
    }
    
    # Fonts
    location ~* \.(woff|woff2|ttf|otf|eot)$ {
        proxy_pass http://plagiatia_backend;
        
        expires 365d;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin *;
        access_log off;
    }
    
    # Manifest PWA et icons
    location = /manifest.json {
        proxy_pass http://plagiatia_backend;
        expires 7d;
        add_header Cache-Control "public";
    }
    
    location = /sw.js {
        proxy_pass http://plagiatia_backend;
        expires 0;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
    
    # Service Worker scope
    location ~ ^/(sw\.js|workbox-.*\.js)$ {
        proxy_pass http://plagiatia_backend;
        expires 0;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
    
    # Robots.txt et sitemap
    location = /robots.txt {
        proxy_pass http://plagiatia_backend;
        add_header Cache-Control "public, max-age=86400";
    }
    
    # Favicon
    location = /favicon.ico {
        proxy_pass http://plagiatia_backend;
        access_log off;
        log_not_found off;
        expires 7d;
    }
    
    # Health check endpoint (sans logs)
    location /api/health {
        proxy_pass http://plagiatia_backend;
        access_log off;
        log_not_found off;
        add_header Cache-Control "no-store";
    }
    
    # Page d'erreur personnalisée
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
        internal;
    }
    
    # Trop de requêtes
    location @login_rate_limited {
        default_type application/json;
        return 429 '{"error":"Trop de tentatives. Réessayez dans quelques minutes.","code":"RATE_LIMITED"}';
    }
}

#===============================================================================
# SNIPPET PROXY (à inclure dans /etc/nginx/snippets/)
#===============================================================================
# Créer ce fichier: /etc/nginx/snippets/plagiatia-proxy.conf
#
# proxy_http_version 1.1;
# proxy_set_header Upgrade $http_upgrade;
# proxy_set_header Connection 'upgrade';
# proxy_set_header Host $host;
# proxy_set_header X-Real-IP $remote_addr;
# proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
# proxy_set_header X-Forwarded-Proto $scheme;
# proxy_set_header X-Forwarded-Host $host;
# proxy_set_header X-Request-ID $request_id;
# 
# proxy_connect_timeout 60s;
# proxy_send_timeout 300s;
# proxy_read_timeout 300s;
# 
# proxy_buffering on;
# proxy_buffer_size 4k;
# proxy_buffers 8 4k;
# proxy_busy_buffers_size 8k;
